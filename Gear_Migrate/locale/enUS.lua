local L = Apollo.GetPackage("Gemini:Locale-1.0").tPackage:NewLocale("Gear_Migrate", "enUS", true)
if not L then return end

         L["GP_FLAVOR"] = "Migrate old data format."
	 L["GP_O_SETTINGS"] = "Migrate"